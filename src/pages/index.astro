---
import BaseHead from '../components/BaseHead.astro';
import Footer from '../components/Footer.astro';
import Header from '../components/Header.astro';
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import { getCollection } from 'astro:content';

const publications = (await getCollection('publications'))
  .filter(p => p.data.featured && !p.data.draft)
  .sort((a,b) => b.data.year - a.data.year);
---

<section class="hero">
  <h1>Mason J. Gray</h1>
  <p class="subtitle">
    Materials physics · condensed matter · computational research
  </p>

  <p class="blurb">
    I work on the physics and engineering of advanced materials, with interests
    spanning condensed matter, experimental systems, and translating
    fundamental research into usable technology.
  </p>
</section>

<section>
  <h2>Selected Publications</h2>

  {publications.length === 0 ? (
    <p>No featured publications yet.</p>
  ) : (
    <ul class="pub-list">
      {publications.map(pub => (
        <li>
          <strong>{pub.data.title}</strong><br />
          <span class="authors">{pub.data.authors}</span><br />
          <em>{pub.data.venue}</em>, {pub.data.year}
        </li>
      ))}
    </ul>
  )}
</section>

<section>
  <h2>Current Interests</h2>
  <ul>
    <li>Topological materials in devices</li>
    <li>Condensed matter phenomena</li>
    <li>Open scientific tooling and research infrastructure</li>
  </ul>
</section>

<section>
  <h2>Links</h2>
  <ul>
    <li><a href="/cv">Curriculum Vitae</a></li>
    <li><a href="/publications">Publications</a></li>
    <li><a href="/projects">Projects</a></li>
    <li><a href="https://chat.masonjgray.com">Matrix Chat</a></li>
  </ul>
</section>
